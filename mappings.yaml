# mappings.yaml
module_mappings:
  source: "terraform-ibm-key-protect"
  inputs:
    # Target input
    resource_group_id:
      # Source mappings
      - module: terraform-ibm-modules/resource-group
        output: resource_group_id
    zone_id:
      - module: terraform-ibm-modules/cbr
        output: zone_id
